# ===================== 项目配置 & MAC 地址管理 =====================
substitutions:
  project_name: "byy1"
  build_time: "Unknown"
  
  # ===== 更改设备 MAC 地址请修改这里 =====
  mac_ct: "488027700000"   # 餐厅
  mac_cw: "F0CF2D700000"   # 次卧
  mac_zzw: "922635700000"  # 主卧
  mac_kkt: "CDAB38700000"  # 客厅

esphome:
  name: ble-2
  friendly_name: "${project_name}"
  comment: "${project_name} - Build: ${build_time}"
  includes:
    - midea_ble_controller.h
  on_boot:
    priority: 600
    then:
      - lambda: |-
          set_device_mac(0, "${mac_ct}");
          set_device_mac(1, "${mac_cw}");
          set_device_mac(2, "${mac_zzw}");
          set_device_mac(3, "${mac_kkt}");
          ble_set_send_function([](const std::string& hex) { id(tx).send_hex(hex); });
          mqtt_set_publish_function([](const char* topic, const char* payload) { id(bemfa).publish(topic, payload); });
          register_entity_sync([](int idx, const MideaDevice& dev) {
              if (idx == 0) {
                  id(ct_light_sw).publish_state(dev.light_on); id(ct_fan_sw).publish_state(dev.fan_on);
                  id(ct_brt_n).publish_state(dev.brightness); id(ct_ct_n).publish_state(dev.color_temp); id(ct_spd_n).publish_state(dev.fan_speed);
              } else if (idx == 1) {
                  id(cw_light_sw).publish_state(dev.light_on); id(cw_fan_sw).publish_state(dev.fan_on);
                  id(cw_brt_n).publish_state(dev.brightness); id(cw_ct_n).publish_state(dev.color_temp); id(cw_spd_n).publish_state(dev.fan_speed);
              } else if (idx == 2) {
                  id(zzw_light_sw).publish_state(dev.light_on); id(zzw_fan_sw).publish_state(dev.fan_on);
                  id(zzw_brt_n).publish_state(dev.brightness); id(zzw_ct_n).publish_state(dev.color_temp); id(zzw_spd_n).publish_state(dev.fan_speed);
              } else if (idx == 3) {
                  id(kkt_light_sw).publish_state(dev.light_on); id(kkt_fan_sw).publish_state(dev.fan_on);
                  id(kkt_brt_n).publish_state(dev.brightness); id(kkt_ct_n).publish_state(dev.color_temp); id(kkt_spd_n).publish_state(dev.fan_speed);
              }
          });

esp32:
  board: esp32dev
  flash_size: 4MB
  framework:
    type: esp-idf
    sdkconfig_options:
      CONFIG_FREERTOS_UNICORE: y
      CONFIG_BT_ENABLED: y
      CONFIG_BT_BLE_ENABLED: y

logger:
  baud_rate: 0
  level: INFO

wifi:
  ssid: "CC"
  password: "chen1122"
  fast_connect: true
  power_save_mode: none
  ap:
    ssid: "BLE-GW Fallback"
    password: "12345678"

captive_portal:
web_server:
  port: 80
ota:
  - platform: esphome
api:
  reboot_timeout: 0s
  services:
    - service: send_raw_hex
      variables:
        hex_data: string
      then:
        - lambda: 'id(tx).send_hex(hex_data);'

external_components:
  - source:
      type: local
      path: components

ble_tx:
  id: tx

esp32_ble:
  io_capability: none
  enable_on_boot: true

bluetooth_proxy:
  active: true
  cache_services: true

# ===================== 巴法云 MQTT =====================
mqtt:
  id: bemfa
  broker: bemfa.com
  port: 9501
  username: "97109945d7e24559b3e4ca7a1f8ef160"
  password: "97109945d7e24559b3e4ca7a1f8ef160"
  client_id: "97109945d7e24559b3e4ca7a1f8ef160"
  keepalive: 30s
  discovery: false
  birth_message:
  will_message:
  on_connect:
    - delay: 2s
    - lambda: 'publish_all_devices();'
  on_message:
    - topic: "ctlight002"
      then:
        - lambda: 'handle_light_mqtt(0, x);'
    - topic: "ctfan003"
      then:
        - lambda: 'handle_fan_mqtt(0, x);'
    - topic: "cwlight002"
      then:
        - lambda: 'handle_light_mqtt(1, x);'
    - topic: "cwfan003"
      then:
        - lambda: 'handle_fan_mqtt(1, x);'
    - topic: "zzwlight002"
      then:
        - lambda: 'handle_light_mqtt(2, x);'
    - topic: "zzwfan003"
      then:
        - lambda: 'handle_fan_mqtt(2, x);'
    - topic: "kktlight002"
      then:
        - lambda: 'handle_light_mqtt(3, x);'
    - topic: "kktfan003"
      then:
        - lambda: 'handle_fan_mqtt(3, x);'

# ===================== BLE 扫描 =====================
esp32_ble_tracker:
  scan_parameters:
    interval: 640ms
    window: 30ms
    active: false
  on_ble_advertise:
    - then:
        - lambda: |-
            auto datas = x.get_manufacturer_datas();
            for (auto &data : datas) parse_ble_advertisement(data.data);

# ===================== 定时任务 =====================
interval:
  - interval: 50ms
    then:
      - lambda: 'tick_ble_queue();'
  - interval: 60s
    then:
      - lambda: 'tick_heartbeat();'

# ===================== 控制开关 =====================
switch:
  - platform: template
    name: "Dining Light"
    id: ct_light_sw
    icon: mdi:ceiling-light
    optimistic: false
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          g_devices[0].light_on = true;
          if (g_devices[0].brightness < 1) g_devices[0].brightness = 50;
          ble_queue_add(g_devices[0].cmd_light_on(), 0);
    turn_off_action:
      - lambda: |-
          g_devices[0].light_on = false;
          ble_queue_add(g_devices[0].cmd_light_off(), 0);
    lambda: 'return g_devices[0].light_on;'
  - platform: template
    name: "Dining Fan"
    id: ct_fan_sw
    icon: mdi:fan
    optimistic: false
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          g_devices[0].fan_on = true;
          if (g_devices[0].fan_speed < 1) g_devices[0].fan_speed = 1;
          ble_queue_add(g_devices[0].cmd_fan_on(), 0);
    turn_off_action:
      - lambda: |-
          g_devices[0].fan_on = false;
          ble_queue_add(g_devices[0].cmd_fan_off(), 0);
    lambda: 'return g_devices[0].fan_on;'
  - platform: template
    name: "Bedroom Light"
    id: cw_light_sw
    icon: mdi:ceiling-light
    optimistic: false
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          g_devices[1].light_on = true;
          if (g_devices[1].brightness < 1) g_devices[1].brightness = 50;
          ble_queue_add(g_devices[1].cmd_light_on(), 0);
    turn_off_action:
      - lambda: |-
          g_devices[1].light_on = false;
          ble_queue_add(g_devices[1].cmd_light_off(), 0);
    lambda: 'return g_devices[1].light_on;'
  - platform: template
    name: "Bedroom Fan"
    id: cw_fan_sw
    icon: mdi:fan
    optimistic: false
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          g_devices[1].fan_on = true;
          if (g_devices[1].fan_speed < 1) g_devices[1].fan_speed = 1;
          ble_queue_add(g_devices[1].cmd_fan_on(), 0);
    turn_off_action:
      - lambda: |-
          g_devices[1].fan_on = false;
          ble_queue_add(g_devices[1].cmd_fan_off(), 0);
    lambda: 'return g_devices[1].fan_on;'
  - platform: template
    name: "Master Light"
    id: zzw_light_sw
    icon: mdi:ceiling-light
    optimistic: false
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          g_devices[2].light_on = true;
          if (g_devices[2].brightness < 1) g_devices[2].brightness = 50;
          ble_queue_add(g_devices[2].cmd_light_on(), 0);
    turn_off_action:
      - lambda: |-
          g_devices[2].light_on = false;
          ble_queue_add(g_devices[2].cmd_light_off(), 0);
    lambda: 'return g_devices[2].light_on;'
  - platform: template
    name: "Master Fan"
    id: zzw_fan_sw
    icon: mdi:fan
    optimistic: false
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          g_devices[2].fan_on = true;
          if (g_devices[2].fan_speed < 1) g_devices[2].fan_speed = 1;
          ble_queue_add(g_devices[2].cmd_fan_on(), 0);
    turn_off_action:
      - lambda: |-
          g_devices[2].fan_on = false;
          ble_queue_add(g_devices[2].cmd_fan_off(), 0);
    lambda: 'return g_devices[2].fan_on;'
  - platform: template
    name: "Living Room Light"
    id: kkt_light_sw
    icon: mdi:ceiling-light
    optimistic: false
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          g_devices[3].light_on = true;
          if (g_devices[3].brightness < 1) g_devices[3].brightness = 50;
          ble_queue_add(g_devices[3].cmd_light_on(), 0);
    turn_off_action:
      - lambda: |-
          g_devices[3].light_on = false;
          ble_queue_add(g_devices[3].cmd_light_off(), 0);
    lambda: 'return g_devices[3].light_on;'
  - platform: template
    name: "Living Room Fan"
    id: kkt_fan_sw
    icon: mdi:fan
    optimistic: false
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          g_devices[3].fan_on = true;
          if (g_devices[3].fan_speed < 1) g_devices[3].fan_speed = 1;
          ble_queue_add(g_devices[3].cmd_fan_on(), 0);
    turn_off_action:
      - lambda: |-
          g_devices[3].fan_on = false;
          ble_queue_add(g_devices[3].cmd_fan_off(), 0);
    lambda: 'return g_devices[3].fan_on;'

# ===================== 调节滑块 =====================
number:
  - platform: template
    name: "Dining Brightness"
    id: ct_brt_n
    min_value: 1
    max_value: 100
    step: 1
    unit_of_measurement: "%"
    icon: mdi:brightness-percent
    set_action:
      - lambda: |-
          g_devices[0].brightness = (int)x;
          g_devices[0].light_on = true;
          ble_queue_add(g_devices[0].cmd_brightness((int)x), 0);
  - platform: template
    name: "Dining ColorTemp"
    id: ct_ct_n
    min_value: 2700
    max_value: 6500
    step: 100
    unit_of_measurement: "K"
    icon: mdi:thermometer
    set_action:
      - lambda: |-
          g_devices[0].color_temp = (int)x;
          ble_queue_add(g_devices[0].cmd_color_temp((int)x), 0);
  - platform: template
    name: "Dining Fan Speed"
    id: ct_spd_n
    min_value: 1
    max_value: 6
    step: 1
    icon: mdi:fan-speed-1
    set_action:
      - lambda: |-
          g_devices[0].fan_speed = (int)x;
          g_devices[0].fan_on = true;
          ble_queue_add(g_devices[0].cmd_fan_on(), 0);
  - platform: template
    name: "Bedroom Brightness"
    id: cw_brt_n
    min_value: 1
    max_value: 100
    step: 1
    unit_of_measurement: "%"
    icon: mdi:brightness-percent
    set_action:
      - lambda: |-
          g_devices[1].brightness = (int)x;
          g_devices[1].light_on = true;
          ble_queue_add(g_devices[1].cmd_brightness((int)x), 0);
  - platform: template
    name: "Bedroom ColorTemp"
    id: cw_ct_n
    min_value: 2700
    max_value: 6500
    step: 100
    unit_of_measurement: "K"
    icon: mdi:thermometer
    set_action:
      - lambda: |-
          g_devices[1].color_temp = (int)x;
          ble_queue_add(g_devices[1].cmd_color_temp((int)x), 0);
  - platform: template
    name: "Bedroom Fan Speed"
    id: cw_spd_n
    min_value: 1
    max_value: 6
    step: 1
    icon: mdi:fan-speed-1
    set_action:
      - lambda: |-
          g_devices[1].fan_speed = (int)x;
          g_devices[1].fan_on = true;
          ble_queue_add(g_devices[1].cmd_fan_on(), 0);
  - platform: template
    name: "Master Brightness"
    id: zzw_brt_n
    min_value: 1
    max_value: 100
    step: 1
    unit_of_measurement: "%"
    icon: mdi:brightness-percent
    set_action:
      - lambda: |-
          g_devices[2].brightness = (int)x;
          g_devices[2].light_on = true;
          ble_queue_add(g_devices[2].cmd_brightness((int)x), 0);
  - platform: template
    name: "Master ColorTemp"
    id: zzw_ct_n
    min_value: 2700
    max_value: 6500
    step: 100
    unit_of_measurement: "K"
    icon: mdi:thermometer
    set_action:
      - lambda: |-
          g_devices[2].color_temp = (int)x;
          ble_queue_add(g_devices[2].cmd_color_temp((int)x), 0);
  - platform: template
    name: "Master Fan Speed"
    id: zzw_spd_n
    min_value: 1
    max_value: 6
    step: 1
    icon: mdi:fan-speed-1
    set_action:
      - lambda: |-
          g_devices[2].fan_speed = (int)x;
          g_devices[2].fan_on = true;
          ble_queue_add(g_devices[2].cmd_fan_on(), 0);
  - platform: template
    name: "Living Room Brightness"
    id: kkt_brt_n
    min_value: 1
    max_value: 100
    step: 1
    unit_of_measurement: "%"
    icon: mdi:brightness-percent
    set_action:
      - lambda: |-
          g_devices[3].brightness = (int)x;
          g_devices[3].light_on = true;
          ble_queue_add(g_devices[3].cmd_brightness((int)x), 0);
  - platform: template
    name: "Living Room ColorTemp"
    id: kkt_ct_n
    min_value: 2700
    max_value: 6500
    step: 100
    unit_of_measurement: "K"
    icon: mdi:thermometer
    set_action:
      - lambda: |-
          g_devices[3].color_temp = (int)x;
          ble_queue_add(g_devices[3].cmd_color_temp((int)x), 0);
  - platform: template
    name: "Living Room Fan Speed"
    id: kkt_spd_n
    min_value: 1
    max_value: 6
    step: 1
    icon: mdi:fan-speed-1
    set_action:
      - lambda: |-
          g_devices[3].fan_speed = (int)x;
          g_devices[3].fan_on = true;
          ble_queue_add(g_devices[3].cmd_fan_on(), 0);

# ===================== 系统传感器 =====================
sensor:
  - platform: uptime
    name: "Uptime"
    update_interval: 60s
  - platform: internal_temperature
    name: "ESP32 Temp"
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 60s
  - platform: wifi_signal
    name: "WiFi dBm"
    id: wifi_signal_db
    update_interval: 60s
  - platform: copy
    source_id: wifi_signal_db
    name: "WiFi Pct"
    filters:
      - lambda: return min(max(2*(x+100.0),0.0),100.0);
    unit_of_measurement: "%"
    accuracy_decimals: 0

binary_sensor:
  - platform: status
    name: "Gateway Online"
    device_class: connectivity

text_sensor:
  - platform: template
    name: "Build Time"
    lambda: 'return {"${build_time}"};'
    update_interval: 3600s
  - platform: version
    name: "ESPHome Ver"
    hide_timestamp: true
  - platform: wifi_info
    ip_address:
      name: "ESP IP"
    ssid:
      name: "ESP SSID"

button:
  - platform: restart
    name: "Restart GW"
    icon: mdi:restart
    entity_category: diagnostic
